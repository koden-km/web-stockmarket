import counter from '../features/counter/counterSlice.js'  // TODO(KM): For learning framework only, delete.
import gameplay from '../features/gameplay/gameplay-slice.js'
import jobs from '../features/jobs/jobs-slice.js'
import stocks from '../features/stocks/stocks-slice.js'

export const reducer = {
  counter,
  gameplay,
  jobs,
  stocks,
}
