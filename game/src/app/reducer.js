import counter from '../features/counter/counterSlice.js'  // TODO(KM): For learning framework only, delete.
import gameplay from '../features/gameplay/gameplaySlice.js'
import jobs from '../features/jobs/jobsSlice.js'
import stocks from '../features/stocks/stocksSlice.js'

export const reducer = {
  counter,
  gameplay,
  jobs,
  stocks,
}
